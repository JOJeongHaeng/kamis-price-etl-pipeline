services:
  - type: web
    name: smartshopping
    runtime: python
    plan: free
    branch: main
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    startCommand: python tools/seed_demo_db.py && uvicorn web.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
